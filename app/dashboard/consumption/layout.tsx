export const metadata = {
  title: "Consumption | CarChronicle",
};

export default function ConsumptionLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
